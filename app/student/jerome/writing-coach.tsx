"use client"; import {useState} from "react";
export function WritingCoach(){const[t,setT]=useState("");const l=t.toLowerCase(),w=t.trim().split(/\s+/).filter(Boolean);const checks=[[w.length>=8,"Clear main idea"],[/because|example|according to|shows/.test(l),"Evidence or example"],[/this means|therefore|however|as a result/.test(l),"Reasoning connects evidence"],[w.length>=25,"Enough development"]];return <section className="writingCoach"><p className="eyebrow">WRITING & ANALYSIS COACH</p><h2>Keep your idea. Strengthen how you say it.</h2><p>Write in your own words first. This coach shows what to add without doing the thinking for you.</p><div><textarea value={t} onChange={e=>setT(e.target.value)} placeholder="I think the author is saying…"/><article><h3>Response check</h3>{checks.map(x=><p className={x[0]?"met":"next"} key={String(x[1])}>{x[0]?"✓":"→"} {x[1]}</p>)}<h3>Stronger structure</h3>{["State the claim.","Add a fact or quotation.","Explain how it proves the claim.","Connect it to a larger meaning."].map((x,i)=><p className="coachStep" key={x}><b>{i+1}</b>{x}</p>)}<button onClick={()=>setT(x=>`${x}${x.trim()?" ":""}This evidence matters because …`)}>＋ Add reasoning starter</button></article></div><aside><b>Academic honesty:</b> Jerome chooses the evidence and final wording.</aside></section>}
